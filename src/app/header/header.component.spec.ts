import { async } from '@angular/core/testing';


describe('<at least one test case>', () => {
    it('should have one unit test case', async(() => {
        expect('unit test').toBe('unit test');
    }));
});
